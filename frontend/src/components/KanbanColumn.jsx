import TaskCard from './TaskCard.jsx'
function KanbanColumn({ column, tasks, users, onStatusChange, onEdit, onDelete }) { function handleDrop(event) { event.preventDefault(); const taskId = Number(event.dataTransfer.getData('text/plain')); if (taskId) onStatusChange(taskId, column.value) } return <section className="kanban-column" onDragOver={(event) => event.preventDefault()} onDrop={handleDrop} aria-labelledby={`${column.value}-heading`}><div className="column-heading"><div><span className={`column-marker column-marker--${column.tone}`} /><div><h2 id={`${column.value}-heading`}>{column.label}</h2><span className="column-subtitle">{tasks.length} {tasks.length === 1 ? 'task' : 'tasks'}</span></div></div><span className="task-count">{tasks.length}</span></div><div className="task-list">{tasks.length === 0 ? <p className="empty-column">Drop a task here</p> : tasks.map((task) => <TaskCard key={task.id} task={task} users={users} onStatusChange={onStatusChange} onEdit={onEdit} onDelete={onDelete} />)}</div></section> }

export default KanbanColumn
