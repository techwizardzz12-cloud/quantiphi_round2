import UserAvatar from './UserAvatar.jsx'
function formatDueDate(date) { if (!date) return 'No due date'; return new Intl.DateTimeFormat('en', { month: 'short', day: 'numeric' }).format(new Date(`${date}T00:00:00`)) }
function TaskCard({ task, users, onStatusChange, onEdit, onDelete }) { const user = users.find((item) => item.id === task.assignedUserId); function handleDragStart(event) { event.dataTransfer.effectAllowed = 'move'; event.dataTransfer.setData('text/plain', String(task.id)) } return <article className="task-card" draggable onDragStart={handleDragStart}><div className="task-card__topline"><span className={`priority priority--${task.priority.toLowerCase()}`}>{task.priority}</span><div className="card-actions"><button type="button" onClick={() => onEdit(task)} aria-label={`Edit ${task.title}`}>Edit</button><button type="button" onClick={() => onDelete(task.id)} aria-label={`Delete ${task.title}`}>×</button></div></div><h3>{task.title}</h3><p>{task.description || 'No description added.'}</p><div className="task-card__meta"><span className="due-date"><span aria-hidden="true">◷</span> {formatDueDate(task.dueDate)}</span>{user ? <span className="assignee"><UserAvatar user={user} small /><span>{user.name}</span></span> : <span className="unassigned">Unassigned</span>}</div><label className="sr-only" htmlFor={`status-${task.id}`}>Move task</label><select className="card-status" id={`status-${task.id}`} value={task.status} onChange={(event) => onStatusChange(task.id, event.target.value)} aria-label={`Move ${task.title}`}><option value="todo">To-Do</option><option value="in_progress">In Progress</option><option value="done">Done</option></select></article> }

export default TaskCard
